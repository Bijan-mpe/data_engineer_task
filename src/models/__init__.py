"""
Data model package.

Modules:
  orm        — SQLAlchemy ORM table definitions (source of truth for the DB schema)
  schemas    — Pydantic models for pipeline-internal data flow (DTOs)
  responses  — Pydantic models for API response serialisation
"""
